# Codebase Context (auto-generated by fondamenta)

> Generated: 2026-02-24 | Framework: auto

## Project Structure

- **Pages:** 3
- **Components:** 23
- **API Routes:** 4
- **Lib Files:** 7
- **DB Models:** 5
- **Enums:** 1

## Detailed Documentation

Full atomic analysis available in `.planning/dependencies/`:
- `pages-atomic.md` — every page with imports, auth, data fetching
- `components-atomic.md` — every component with state, hooks, used-by
- `api-routes-atomic.md` — every API route with methods, auth, models
- `lib-atomic.md` — every utility with exports, env vars
- `schema-crossref-atomic.md` — all DB models, fields, relations
- `component-graph.md` — visual dependency tree
- `DEPENDENCY-MAP.md` — impact areas and test checklists

## Key Routes

- `app/dashboard/page.tsx` → `app/dashboard/page.tsx` (server)
- `app/page.tsx` → `app/page.tsx` (server)
- `app/products/page.tsx` → `app/products/page.tsx` (server)

## DB Models

- `User` — fields: id, email, name, orders, createdAt, ... | 1 relations
- `Product` — fields: id, name, description, price, stock, ... | 2 relations
- `Order` — fields: id, user, userId, items, total, ... | 2 relations
- `OrderItem` — fields: id, order, orderId, product, productId, ... | 2 relations
- `Category` — fields: id, name, products | 1 relations

## Fragile Zones

When modifying these areas, test ALL connected flows:

- **Auth** — 1 protected routes. Changes affect all authenticated pages.
- **Layout** — RootLayout. Changes affect all pages.

